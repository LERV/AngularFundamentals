import { OwnIfDirective } from './own-if.directive';

describe('OwnIfDirective', () => {
  it('should create an instance', () => {
    const directive = new OwnIfDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
